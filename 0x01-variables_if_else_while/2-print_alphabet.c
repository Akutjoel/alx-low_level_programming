#include <stdio.h>
#include <string.h>
/**
 * main - to print alphabet in lower case
 *
 * Return:0 (success)
 */
int main(void)
{
	for (char alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	return (0);
}
